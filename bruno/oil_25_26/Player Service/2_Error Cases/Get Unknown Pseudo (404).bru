meta {
  name: Get Unknown Pseudo (404)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/players/pseudo/UnknownPlayerXYZ
  body: none
  auth: none
}

tests {
  test("Status code is 404", function () {
      expect(res.getStatus()).to.equal(404);
  });
}
