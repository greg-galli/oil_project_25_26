meta {
  name: 5. Get Player By ID (Verify Delete)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/players/{{playerId}}
  body: none
  auth: none
}

tests {
  test("Status code is 404", function () {
      expect(res.getStatus()).to.equal(404);
  });
}
