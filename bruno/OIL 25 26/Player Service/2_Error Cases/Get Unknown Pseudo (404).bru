meta {
  name: Get Unknown Pseudo (404)
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/api/players/pseudo/UnknownPlayerXYZ
  body: none
  auth: none
}

tests {
  test("Status code is 404", function () {
      expect(res.getStatus()).to.equal(404);
  });
}
